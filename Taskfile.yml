version: '3'

tasks:

  build:
    cmds:
      - clang++ -std=c++11 -stdlib=libc++ -lglfw3 -framework CoreVideo -framework OpenGL -framework IOKit -framework Cocoa -framework Carbon ./main.cpp -o main

  build:example:
    cmds:
      - clang++ -std=c++11 -stdlib=libc++ -lglfw3 -framework CoreVideo -framework OpenGL -framework IOKit -framework Cocoa -framework Carbon ./example.cpp

  run:
    cmds:
     - ./main.out

  run:example:
    cmds:
     - ./a.out